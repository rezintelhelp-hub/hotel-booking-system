This widget works inside an Advanced Form. Because it also has editable zones it will need to be grouped before it can go in the editable zone of the Advanced Form. 

The Form Page widget can group Form Fields into sections that let the user fill out the form in chunks. Simply add consecutive instances of this widget inside the Advanced Form editable zone (using Groups) and then add your form fields inside the Form Page editable zone. 
