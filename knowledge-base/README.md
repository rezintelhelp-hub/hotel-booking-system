# GAS Knowledge Base

This folder contains help articles for the GAS AI Assistant.
One markdown file per topic.
Add new .md files here as new features are built.
The /api/ai-chat endpoint reads these to answer client questions.
